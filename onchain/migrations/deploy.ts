import * as anchor from "@coral-xyz/anchor";

module.exports = async function deploy(_provider: anchor.AnchorProvider) {
  // Keep empty for now. Program is deployed with `anchor deploy`.
  // Frontend migration to on-chain calls is tracked in root README/onchain README.
};
